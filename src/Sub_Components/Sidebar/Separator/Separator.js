import React from 'react';
import './Separator.css'; // Make sure to create a corresponding CSS file

const Separator = () => {
  return <hr className="sidenav-separator" />;
}

export default Separator;
